# CI/CD Pipeline for Printing Service
name: Printing Service CI/CD

on:
  push:
    branches: [main, develop, 'release/*']
    paths:
      - 'printing-service/**'
      - 'wms-shared-libs/**'
      - '.github/workflows/printing-service.yml'
      - '.github/workflows/reusable-build.yml'
      - 'pom.xml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'printing-service/**'
      - 'wms-shared-libs/**'
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy to environment'
        required: false
        type: boolean
        default: false
      environment:
        description: 'Target environment'
        required: false
        type: choice
        options:
          - dev
          - staging
          - prod
        default: 'dev'

jobs:
  build-and-deploy:
    name: Printing Service
    uses: ./.github/workflows/reusable-build.yml
    with:
      service-name: printing-service
      service-module: printing-service/printing-app
      port: '8091'
      java-version: '17'
      run-tests: true
      push-image: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
      deploy: ${{ github.event.inputs.deploy == 'true' || (github.event_name == 'push' && github.ref == 'refs/heads/main') }}
      environment: ${{ github.event.inputs.environment || 'dev' }}
    secrets: inherit

  integration-tests:
    name: Karate E2E Tests
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Run Karate E2E Tests
        run: |
          mvn test -pl printing-service/printing-test \
            -Dkarate.options="--tags @smoke" \
            -B
        continue-on-error: true

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: printing-e2e-test-results
          path: printing-service/printing-test/target/karate-reports/
