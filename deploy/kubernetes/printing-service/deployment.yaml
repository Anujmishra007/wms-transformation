---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: printing-service
  labels:
    app.kubernetes.io/name: printing-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: wms
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: printing-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: printing-service
        app.kubernetes.io/component: service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8091"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: wms-service-account
      containers:
        - name: printing-service
          image: IMAGE_TAG
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8091
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "8091"
            - name: SPRING_APPLICATION_NAME
              value: "printing-service"
          envFrom:
            - configMapRef:
                name: wms-common-config
            - secretRef:
                name: wms-db-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8091
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8091
            initialDelaySeconds: 30
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: printing-service
  labels:
    app.kubernetes.io/name: printing-service
spec:
  type: ClusterIP
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: printing-service

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: printing-service-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: printing-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
